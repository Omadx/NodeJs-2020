<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body

        h2 Reestablecer Password
        p Hola, has solicitado reestablecer password, haz click en el siguiente enlace
        a(href=`${resetUrl}`) Ir a Resetear password 
        p Si no puedes acceder a este, visita #{resetUrl}
        p Si no solicitaste este email puedes ignorarlo 