extends layout

include mixins/crearProyecto

block contenido
    .contenido-principal    
        h1 #{nombrePaginas}

        pre= vardump(proyecto)

        if errores
            each error in errores
                .alerta= error.texto
        +crearProyecto(proyecto)
